# Reloading the server

uvicorn main:app --reload

# Building SyriaGPT on Docker

docker build -t syria-gpt .

# Running Docker Container at port 8000

docker run -p 9000:9000 syria-gpt

# Server URL: http://localhost:9000 or http://127.0.0.1:9000

PostgreSQL Admin: http://localhost:5050

# Composing container for first time

docker-compose up --build